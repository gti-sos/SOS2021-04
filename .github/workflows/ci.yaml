name: CI

on:
  push:
    branches: [ main ]


jobs:
  integration_edex:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm start &
      - run: npx newman run tests/integration_edex.json

  integration_poverty_risks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm start &
      - run: npx newman run tests/integration_poverty_risks.json 